<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Shared by the settings window and the welcome window, which both offer the
        same Start with Windows switch and should not be able to draw it two ways.

        Expects the window merging it to define ControlAltFillBrush,
        ControlStrongStrokeBrush, AccentFillBrush and TextOnAccentBrush. They are
        looked up dynamically, so each window keeps its own theme swap.
    -->

    <!-- WPF has no ToggleSwitch, so a CheckBox is retemplated as one: 40x20
         track, 12px knob, 4px inset. The knob slides rather than jumping:
         an instant snap is the tell that a switch is hand-made. -->
    <Style x:Key="ToggleSwitch" TargetType="CheckBox">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Width="40" Height="20" Background="Transparent">
                        <Border x:Name="Track"
                                CornerRadius="10"
                                Background="{DynamicResource ControlAltFillBrush}"
                                BorderBrush="{DynamicResource ControlStrongStrokeBrush}"
                                BorderThickness="1"
                                SnapsToDevicePixels="True" />
                        <Ellipse x:Name="Knob"
                                 Width="12" Height="12"
                                 HorizontalAlignment="Left"
                                 Margin="4,0,0,0"
                                 Fill="{DynamicResource ControlStrongStrokeBrush}">
                            <Ellipse.RenderTransform>
                                <TranslateTransform x:Name="KnobShift" X="0" />
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background"
                                    Value="{DynamicResource AccentFillBrush}" />
                            <Setter TargetName="Track" Property="BorderBrush"
                                    Value="Transparent" />
                            <Setter TargetName="Knob" Property="Fill"
                                    Value="{DynamicResource TextOnAccentBrush}" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="KnobShift"
                                            Storyboard.TargetProperty="X"
                                            To="20" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="KnobShift"
                                            Storyboard.TargetProperty="X"
                                            To="0" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.36" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
